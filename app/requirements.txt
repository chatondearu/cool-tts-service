# TTS Server - Python Dependencies
#
# vllm is intentionally omitted from vllm-omni wheels (upstream entrypoint conflict);
# it must be installed explicitly. Voxtral needs vllm-omni 0.18+ (use rc on PyPI if stable missing).

vllm-omni==0.18.0rc1
vllm
mistral-common

python-dotenv==1.0.0
loguru==0.7.2
huggingface-hub>=0.26.0
