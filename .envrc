# If `nix develop` / `nix print-dev-env` fails with CXXABI_1.3.15, the parent shell
# likely has a stale gcc-*-lib on LD_LIBRARY_PATH. Try: direnv reload
# or: env -u LD_LIBRARY_PATH nix develop
use flake
